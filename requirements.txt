# Rig Builder Dependencies

# Core UI Framework (standalone)
PySide6>=6.0.0

# Markdown rendering for module docs
markdown>=3.0.0

# Client-server networking (Host-agnostic execution)
pyzmq>=24.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
